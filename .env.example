# Anthropic API Key (required)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Output directory (optional, defaults to ~/.blog-builder)
BLOG_BUILDER_OUTPUT=~/.blog-builder

# Default number of articles to generate (optional, defaults to 10)
BLOG_BUILDER_ARTICLE_COUNT=10

# Rate limiting delay in ms between requests (optional, defaults to 1000)
BLOG_BUILDER_RATE_LIMIT=1000

# Log level: debug, info, warn, error (optional, defaults to info)
BLOG_BUILDER_LOG_LEVEL=info
